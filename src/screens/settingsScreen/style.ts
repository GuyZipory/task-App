import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  rootContainer: {
    backgroundColor: 'white',
    flex: 1,
    paddingBlockStart: 20,
    alignItems: 'center',
  },
  button: {backgroundColor: '#0af', borderRadius: 6, height: 44, width: '90%'},
});
